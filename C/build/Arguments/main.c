/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

/* 
 * File:   main.c
 * Author: apean
 *
 * Created on 10 décembre 2020, 09:21
 */

#include <stdio.h>
#include <stdlib.h>
#include "fonctions.h"
#include <math.h>

/*
 * 
 */
int main(int argc, char** argv) {
/*
    float a,b,c,delta;
    
    if(argc!=4){
        printf("usage : %s a b c\n",argv[0]);
        exit(0);
    }
    
    sscanf(argv[1],"%f",&a);
    if(a==0){
        printf("usage : %s a b c\n a doit etre différent de 0",argv[0]);
        exit(0);
    }
     sscanf(argv[2],"%f",&b);
     sscanf(argv[3],"%f",&c);
    
     delta=b*b-4*a*c;
     if(delta<0){
         printf("Pas de racine pour le polynome (%.2f)x² + (%.2f)x + (%.2f) = 0\n",a,b,c);
         
     }else{
         printf("Le polynome (%.2f)x² + (%.2f)x + (%.2f) = 0\n a pour racines : ",a,b,c);
         printf("x1 = %f \n x2 = %f \n",(-b-sqrt(delta))/(2*a),(-b+sqrt(delta))/(2*a));
     }
     
   */
    
    //-------------------------------TD 1-----------------------
   /* int i;
    for(i=0;i<argc;i++){
       printf("argv(%d) = %s\n",i,argv[i]); 
    }
    
    
   */ 
    
    
    return (EXIT_SUCCESS);
}

